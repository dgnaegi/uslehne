{
  "name": "uslehne",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix frontend --include=dev && npm run build --prefix frontend && npm prune --prefix frontend --omit=dev && npm install --prefix backend --include=dev && npm run build --prefix backend && npm prune --prefix backend --omit=dev",
    "start": "node backend/dist/index.js",
    "db:migrate": "npm run db:migrate --prefix backend"
  }
}
